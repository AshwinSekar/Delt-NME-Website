import {
  progressBar
} from '../../../helpers/progress-bar';
import { module, test } from 'qunit';

module('ProgressBarHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = progressBar(42);
  assert.ok(result);
});
